# Phase 09 Plan — Self-Improving CLAUDE.md

## Goal
Create project-level CLAUDE.md with Obsidian conventions. After each research session, prompt for patterns to remember. Future sessions consult CLAUDE.md for better query formulation.

## Steps

### 09-01: Create CLAUDE.md
- [ ] Create `CLAUDE.md` in project root
- [ ] Add Obsidian conventions section:
  - `[[double bracket links]]` for related content
  - `![[embeds]]` for embedded content
  - `#tags` for inline tagging
  - YAML frontmatter format
- [ ] Add Research Patterns section (empty, to be filled)
- [ ] Add Session Notes section

### 09-02: Pattern Review After Sessions
- [ ] In `research_utils.py`, add `review_session_patterns(session_id, db)`
- [ ] After research session completes, generate pattern review prompt
- [ ] Call `update_claude_md(pattern_notes)` to append to CLAUDE.md
- [ ] Simple append approach — add bullet under "## Session Notes"

### 09-03: Query Formulation Consulting
- [ ] Before Tavily search, check CLAUDE.md for "What Works" patterns
- [ ] Use patterns to improve query formulation
- [ ] Add `consult_claude_md(section)` utility

### 09-04: Tests
- [ ] `tests/test_self_improving.py` — test CLAUDE.md read/write

## Success Criteria
- CLAUDE.md created with Obsidian conventions
- After research sessions, patterns appended to CLAUDE.md
- Future queries consult CLAUDE.md for better results
- No API keys needed — pure text file approach
